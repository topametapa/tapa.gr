---
layout: base.njk
title: Blog | Insights για Web Development
description: Άρθρα, οδηγοί και insights για κατασκευή ιστοσελίδων, SEO, performance optimization και digital marketing στην Ελλάδα.
eleventyExcludeFromCollections: true
pagination:
  data: collections.posts
  size: 20
  reverse: true
  alias: posts
permalink: "{% if pagination.pageNumber == 0 %}/blog/{% else %}/blog/page/{{ pagination.pageNumber + 1 }}/{% endif %}"
---

<section class="section bg-slate-50 border-b border-slate-200">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto text-center">
      <h1 class="text-4xl sm:text-5xl font-bold tracking-tight text-slate-900">Blog & Insights</h1>
      <p class="section-subtitle mx-auto text-center">Πρακτικές συμβουλές για ιστοσελίδες, eshop, SEO και performance</p>
      {% if pagination.pageNumber > 0 %}
      <p class="mt-2 text-sm text-slate-500">Σελίδα {{ pagination.pageNumber + 1 }} από {{ pagination.pages.length }}</p>
      {% endif %}
    </div>
  </div>
</section>

<section class="section">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    {# Total posts count #}
    <div class="mb-8 text-sm text-slate-500">
      {{ collections.posts.length }} άρθρα συνολικά
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {% for post in posts %}
      <article class="card group flex flex-col transition-transform duration-300 hover:-translate-y-1 overflow-hidden p-0">
        {# Abstract tech image placeholder #}
        <div class="h-44 bg-gradient-to-br from-slate-100 via-slate-50 to-brand/10 relative overflow-hidden">
          <div class="absolute inset-0 opacity-30">
            <svg class="w-full h-full" viewBox="0 0 400 176" fill="none" aria-hidden="true">
              <circle cx="350" cy="30" r="100" fill="currentColor" class="text-brand/20"/>
              <circle cx="50" cy="150" r="70" fill="currentColor" class="text-slate-200"/>
              <path d="M0 88 Q100 48 200 88 T400 88" stroke="currentColor" stroke-width="2" class="text-brand/30" fill="none"/>
              <path d="M0 110 Q100 70 200 110 T400 110" stroke="currentColor" stroke-width="1" class="text-slate-300" fill="none"/>
            </svg>
          </div>
          <div class="absolute bottom-4 left-4">
            {% if post.data.category %}
            <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-semibold bg-white/90 text-brand shadow-sm">
              {{ post.data.category }}
            </span>
            {% endif %}
          </div>
        </div>

        <div class="flex-1 p-6">
          <div class="flex items-center gap-3 mb-3">
            <time class="text-xs text-slate-400" datetime="{{ post.date | dateIso }}">
              {{ post.date | dateFormat }}
            </time>
            {% if post.data.readingTime %}
            <span class="text-xs text-slate-400">· {{ post.data.readingTime }} min</span>
            {% endif %}
          </div>

          <h2 class="text-xl font-bold text-slate-900 group-hover:text-brand transition-colors">
            <a href="{{ post.url }}" class="block">
              {{ post.data.title }}
            </a>
          </h2>

          {% if post.data.excerpt %}
          <p class="mt-3 text-slate-600 text-sm line-clamp-3">{{ post.data.excerpt }}</p>
          {% endif %}
        </div>

        <div class="px-6 pb-6 pt-4 border-t border-slate-100 mt-auto">
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-2">
              <div class="w-8 h-8 bg-brand rounded-full flex items-center justify-center text-slate-900 font-semibold text-xs">
                MT
              </div>
              <span class="text-xs text-slate-500">{{ post.data.author | default("Michael Topouzis") }}</span>
            </div>
            <a href="{{ post.url }}" class="text-xs font-semibold text-brand hover:text-brand/80 transition-colors">
              Διαβάστε →
            </a>
          </div>
        </div>
      </article>
      {% else %}
      <div class="col-span-full text-center py-12">
        <p class="text-slate-500">Δεν υπάρχουν άρθρα ακόμα. Έρχονται σύντομα!</p>
      </div>
      {% endfor %}
    </div>

    {# Pagination Navigation #}
    {% if pagination.pages.length > 1 %}
    <nav class="mt-16 flex items-center justify-center gap-2" aria-label="Πλοήγηση σελίδων">
      {% if pagination.href.previous %}
      <a href="{{ pagination.href.previous }}" class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-slate-600 bg-white border border-slate-200 rounded-lg hover:bg-slate-50 hover:text-brand transition-colors">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
        Προηγούμενη
      </a>
      {% endif %}

      <div class="flex items-center gap-1">
        {% for pageEntry in pagination.pages %}
        {% set pageIndex = loop.index0 %}
        {% if pageIndex == pagination.pageNumber %}
        <span class="w-10 h-10 flex items-center justify-center text-sm font-semibold text-white bg-brand rounded-lg">
          {{ pageIndex + 1 }}
        </span>
        {% else %}
        <a href="{{ pagination.hrefs[pageIndex] }}" class="w-10 h-10 flex items-center justify-center text-sm font-medium text-slate-600 bg-white border border-slate-200 rounded-lg hover:bg-slate-50 hover:text-brand transition-colors">
          {{ pageIndex + 1 }}
        </a>
        {% endif %}
        {% endfor %}
      </div>

      {% if pagination.href.next %}
      <a href="{{ pagination.href.next }}" class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-slate-600 bg-white border border-slate-200 rounded-lg hover:bg-slate-50 hover:text-brand transition-colors">
        Επόμενη
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
        </svg>
      </a>
      {% endif %}
    </nav>
    {% endif %}
  </div>
</section>

<section class="section bg-slate-900">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-2xl sm:text-3xl font-bold text-white">Θέλετε να λαμβάνετε τα νέα μας;</h2>
    <p class="mt-4 text-slate-400 max-w-xl mx-auto">Εγγραφείτε στο newsletter μας για tips, case studies και web development insights.</p>
    <form class="mt-8 flex flex-col sm:flex-row gap-4 justify-center max-w-md mx-auto" action="https://formspree.io/f/YOUR_FORM_ID" method="POST">
      <label for="newsletter-email-blog" class="sr-only">Email</label>
      <input
        type="email"
        id="newsletter-email-blog"
        name="email"
        required
        placeholder="you@example.com"
        class="flex-1 px-4 py-3 rounded-lg bg-slate-800 border border-slate-700 text-white placeholder-slate-500 focus:outline-none focus:border-brand focus:ring-1 focus:ring-brand"
      >
      <button type="submit" class="btn btn-primary whitespace-nowrap">
        Εγγραφή
      </button>
    </form>
  </div>
</section>
